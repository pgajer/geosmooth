# Migration notes

Use this directory for notes about the split from `gflow`, namespace migration,
API compatibility, dependency boundaries, and cleanup sequencing.

Retained historical bundles belong under `dev/archive/`, not here. This
directory should explain migrations and point to retained evidence, while the
archive stores selected legacy snapshots.
