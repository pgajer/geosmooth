# Tier-2 Audit - Binary Path and Numerical Hygiene

Date: 2026-06-11
Auditor: Codex, independent auditor
Worktree: `/Users/pgajer/current_projects/geosmooth-t2`
Branch: `codex/geosmooth-t2-binary-hygiene`
Audited commit: `53a0b0c44b7e8c2364a05862a87b21c92d208a0b`

## Verdict

- **E2.14 local logistic robustness under separation: accept.** The gate tests the near-separable one-flip case and the exactly separable no-flip case, asserts the per-iteration deviance trajectory to the spec tolerance `1e-8`, checks bounded finite output for the converged case, and checks the documented telemetered fallback for exact separation. Both required mutations made the gate red.
- **E2.12 binary selection metric and log-loss clipping: accept-with-required-fixes.** The delivered R-backend gate is mutation-qualified and correctly pins the deployed clipped metric and the `1e-6` log-loss clip. However, production source still allows a legal Bernoulli C++ CV path that selects on raw aggregate RMSE because it has no per-point CV predictions. The implementer disclosed this. To make the E2.12 claim unconditional, either force all Bernoulli fits to the R backend, or implement deployed clipped scoring for the C++ path. Until then, the accepted gate covers the R binary path, not every legal Bernoulli configuration.
- **E2.13 ridge-penalty structure alignment: deferred pending contract §G4.** This is absent because the contract says E2.13 touches Tier-0 defaults and needs the intended default state confirmed before changing it. I do not treat the absence as a defect.

## Baseline Runs

Clean start checks:

- `pwd`: `/Users/pgajer/current_projects/geosmooth-t2`
- `git rev-parse HEAD`: `53a0b0c44b7e8c2364a05862a87b21c92d208a0b`
- `git status --short`: empty before audit runs and after mutation restoration.

Focused gates:

- `tests/testthat/test-lps-binary-separation.R`: 43 pass, 0 fail.
- `tests/testthat/test-lps-binary-metric-consistency.R`: 38 pass, 0 fail.

Fresh execution artifact:

- Command: `EXECUTOR="auditor-agent-t2" bash scripts/ci/run_tier2_execution_artifact.sh`
- Bundle: `dev/methods/lps/audit_artifacts/tier2_20260611T210313Z`
- Bundle HEAD: `53a0b0c44b7e8c2364a05862a87b21c92d208a0b`
- Bundle clean tree: `true`; post-study clean tree: `true`
- Test summary: `tests=22 failed=0 error=0 warning=0 skipped=1`
- Gate contexts: `E0.1`, `E0.2`, `E0.3a`, `E0.4`, `E0.5`, `E0.6`, `E0.7`, `E0.8`, `E2.12`, `E2.12a`, `E2.12b`, `E2.14`
- Probe: `probe_rc: 0`
- Cross-clip study: `study_rc: 0`, reported as diagnostic, not gated.
- Bundle records `git_head`, `git_status`, `sessionInfo`, `blas`, source checksums, raw test results, probe outputs, study outputs, and bundle checksums.

## Mutation Results

| Gate | Property | Transient mutation in `R/lps.R` | Result |
|---|---|---|---|
| E2.14 monotone trajectory | Step-halving prevents deviance increases beyond `1e-8` | Changed `max.step.halvings <- 30L` to `0L` | **Red**. Near-separable solve returned `status = "step_halving_failed"` instead of `ok`; bounded prediction assertions failed. |
| E2.14 fallback | Exact separation uses documented telemetered fallback | Replaced final failed-solve fallback call in `.klp.fit.logistic.prob.design()` with `NA_real_` | **Red**. Exact-separation fitted value was `NA`; telemetry counts for `attempted`, `fallback.path`, and `event.rate.fallback` failed. |
| E2.12 clipped metric | Bernoulli selection score equals deployed clipped Brier metric | Changed `cv.brier.observed` to use raw `pred[, j]` instead of `.klp.response.scale(pred[, j], outcome.family)` | **Red**. The constructed case selected `(support=8, degree=0)` instead of `(60,2)` and the deployed-metric equality failed. |
| E2.12 clip value | Log-loss clip pinned at `1e-6` | Changed `.klp.clip.probability` default from `1e-6` to `1e-15` | **Red**. The formals/bounds assertions failed, `logloss(c(1,0), c(0,1))` became `34.5` instead of `13.8`, and observed log-loss no longer matched the `1e-6` recomputation. |

Each mutation was reverted with `git checkout -- R/lps.R`; focused tests were rerun green afterward.

## Spec Fidelity

E2.14:

- Uses the required deviance tolerance `1e-8`.
- Tests both required cases: near-separable one flipped label and exact separable no-flip.
- Asserts the deviance sequence, not only endpoint behavior.
- Exact separation reaches `status = "not_converged"` at the iteration cap and then the fitting layer records the fallback telemetry.

E2.12:

- The R-backend gate asserts the selected score equals the deployed clipped metric, recomputed from stored CV predictions.
- The motivating case is a real ranking flip, not a tie.
- The log-loss clip is pinned at `1e-6`.
- The `1e-15` instability is demonstrated by a unique confident-wrong binder.
- Cross-clip stability over `{1e-6, 1e-3}` is a study/reporting output only; the report says it reselects at `1e-3`, and the tests do not require invariance.

Required-fix scope issue:

- `audit_contracts/tiers1to4/t2_spec_questions_implementer_2026-06-11.md` item 8 identifies a reachable legal C++ path for Bernoulli (`design.basis = "monomial"`, singleton ridge `0`, `ridge.condition.max = Inf`, coordinates). `R/lps.R` intentionally falls back to `score.column <- "cv.rmse.observed"` when `outcome.family = "bernoulli"` and the C++ CV path returns no per-point predictions.
- I reproduced the corner: explicit `backend = "cpp"` on the E2.12 fixture selected `(support=8, degree=0)`, `backend.used=cpp`, `cv.predictions.null=TRUE`. This is the old raw-score behavior. It is not covered by the R-backend E2.12 gate and should be resolved before declaring E2.12 universal.

## Reproduced Numbers

I independently reran the E2.12a fixture from the test specification and recomputed the deployed Brier score from the raw CV-prediction matrix without package helpers.

- Raw-score argmin: candidate 1, `(support=8, degree=0)`, raw Brier `0.075078359779`, clipped Brier `0.075078359779`.
- Deployed clipped argmin: candidate 4, `(support=60, degree=2)`, raw Brier `0.075653479276`, clipped Brier `0.072936917072`.
- Max residual between manual clipped Brier and `cv.table$cv.brier.observed`: `1.39e-17`.
- Out-of-range raw CV predictions: `263`.

I also checked the bundle's E2.14 realized values:

- Near-separable: `status=ok`, `iterations=9`, `step.halvings=1`, max deviance increase `1.77635683940025e-15`, prediction `0.124668166032953`.
- Exactly separable: `status=not_converged`, `iterations=50`, finite monotone trace, fallback mean `0.0631513478491102`.

## Bundle Validity

The fresh bundle is bound to the audited clean commit and includes source checksums, session info, BLAS info, raw test outputs, probe outputs, cross-clip study outputs, and bundle checksums. The execution manifest records `tree_clean: true`, `testthat_rc: 0`, `probe_rc: 0`, `study_rc: 0`, and `tree_clean_post_study: true`.

The gate coverage includes Tier-0 contexts and Tier-2 E2.12/E2.14 contexts. The single skip is the known E0.3a skip inherited from the Tier-0 battery.

## Handoff Honesty

The implementer handoffs were mostly accurate:

- The E2.14 handoff's numerical claims match the fresh bundle and my mutation results.
- The E2.12 handoff's numerical claims match the fresh bundle and my reproduced rank flip.
- The E2.12 handoff honestly discloses the C++ CV limitation, the corrected binder-dominance operationalization, the non-gated cross-clip study, and the stale exploratory count in an earlier commit message.

The main unresolved issue is not hidden by the implementer: the C++ Bernoulli path remains a documented production-scope exception. I am making it a required fix because the frozen contract claim is "Bernoulli-mode selection scores the deployed metric"; the current source only guarantees that on the R CV path.

## Reviewed Diff

Reviewed production diff from Tier-0 base `b86b796` through audited tip `53a0b0c`:

- `R/lps.R`: 157 lines changed. Relevant changes include `keep.cv.predictions`, `1e-6` clip pin, Bernoulli selection column switch, R-path deployed Brier computation, logistic solver step-halving/deviance trace, and fallback telemetry.
- `tests/testthat/test-lps-binary-separation.R`: new E2.14 gate.
- `tests/testthat/test-lps-binary-metric-consistency.R`: new E2.12 gate.
- `scripts/ci/run_tier2_execution_artifact.sh`: new Tier-2 bundle harness.
- Handoffs: `dev/methods/lps/handoffs/phase/e2_14_implementer_handoff_2026-06-11.md`, `dev/methods/lps/handoffs/phase/e2_12_implementer_handoff_2026-06-11.md`.

I did not commit, push, or leave any mutation in `R/lps.R`.
