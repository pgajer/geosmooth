# LCov development workspace

`lcov` is the development workspace for chart-aware local covariance and local
association material.

Use this method workspace for LCov-specific specifications, audits, reports,
and execution history. Cross-method local-chart or benchmark material should
live under `dev/shared/` and be linked from here.
