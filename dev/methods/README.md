# Method workspaces

Use this directory for method-specific execution history: audit evidence,
contracts, reports, runs, results, handoffs, and status records.

Durable explanatory notes belong under `dev/notes/`.
