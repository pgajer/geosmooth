% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_values.R
\name{model-values}
\alias{model-values}
\alias{fitted.lps}
\alias{residuals.lps}
\alias{fitted.malps}
\alias{residuals.malps}
\alias{fitted.ps_lps}
\alias{residuals.ps_lps}
\alias{fitted.chart_kernel}
\alias{residuals.chart_kernel}
\alias{fitted.local_likelihood}
\alias{residuals.local_likelihood}
\alias{fitted.lpl_tf}
\alias{residuals.lpl_tf}
\alias{fitted.slpl_tf}
\alias{residuals.slpl_tf}
\alias{fitted.metric.graph.lowpass.fit}
\alias{residuals.metric.graph.lowpass.fit}
\alias{fitted.metric.graph.lowpass.refit}
\alias{residuals.metric.graph.lowpass.refit}
\alias{fitted.ssrhe.hessian.fit}
\alias{residuals.ssrhe.hessian.fit}
\alias{fitted.ssrhe.hessian.refit}
\alias{residuals.ssrhe.hessian.refit}
\alias{fitted.ssrhe.hessian.l1.fit}
\alias{residuals.ssrhe.hessian.l1.fit}
\alias{fitted.graph.trend.filtering.fit}
\alias{residuals.graph.trend.filtering.fit}
\alias{fitted.pttf.trend.filtering.fit}
\alias{residuals.pttf.trend.filtering.fit}
\alias{fitted.harmonic_smoother}
\title{Extract Stored Fitted Values and Response Residuals}
\usage{
\method{fitted}{lps}(object, ...)

\method{residuals}{lps}(object, ...)

\method{fitted}{malps}(object, ...)

\method{residuals}{malps}(object, ...)

\method{fitted}{ps_lps}(object, ...)

\method{residuals}{ps_lps}(object, ...)

\method{fitted}{chart_kernel}(object, ...)

\method{residuals}{chart_kernel}(object, ...)

\method{fitted}{local_likelihood}(object, ...)

\method{residuals}{local_likelihood}(object, ...)

\method{fitted}{lpl_tf}(object, ...)

\method{residuals}{lpl_tf}(object, ...)

\method{fitted}{slpl_tf}(object, ...)

\method{residuals}{slpl_tf}(object, ...)

\method{fitted}{metric.graph.lowpass.fit}(object, ...)

\method{residuals}{metric.graph.lowpass.fit}(object, ...)

\method{fitted}{metric.graph.lowpass.refit}(object, ...)

\method{residuals}{metric.graph.lowpass.refit}(object, ...)

\method{fitted}{ssrhe.hessian.fit}(object, ...)

\method{residuals}{ssrhe.hessian.fit}(object, ...)

\method{fitted}{ssrhe.hessian.refit}(object, ...)

\method{residuals}{ssrhe.hessian.refit}(object, ...)

\method{fitted}{ssrhe.hessian.l1.fit}(object, ...)

\method{residuals}{ssrhe.hessian.l1.fit}(object, ...)

\method{fitted}{graph.trend.filtering.fit}(object, ...)

\method{residuals}{graph.trend.filtering.fit}(object, ...)

\method{fitted}{pttf.trend.filtering.fit}(object, ...)

\method{residuals}{pttf.trend.filtering.fit}(object, ...)

\method{fitted}{harmonic_smoother}(object, ...)
}
\arguments{
\item{object}{A supported fitted model; see Methods below.}

\item{...}{Reserved; unused arguments are rejected.}
}
\value{
The stored vector or matrix, or response residuals of the same shape.
}
\description{
Use \code{fitted(object)} for the stored response-scale fitted values, preserving
vector or matrix shape, row order, names, and missing values. It does not
recompute a fit or imply prediction at new points. For \code{harmonic.smoother()}
results it returns \code{harmonic_predictions}. The basic
\code{perform.harmonic.smoothing()} result remains an unclassed list, accessed
through \verb{$harmonic_predictions}; density probability masses remain \verb{$rho}.
}
\details{
\code{residuals(object)} returns stored response residuals where available,
including missing-label masks. Otherwise it computes \code{y - fitted.values}
only when the evaluation coordinates equal the training coordinates.
Residuals at different evaluation points are undefined and cause an error,
even when training and evaluation have the same number of rows. Binomial
residuals are on the response (probability) scale, not Pearson or deviance
residuals. These accessors do not change which refit results are reusable.
}
\examples{
X <- matrix(seq(0, 1, length.out = 20), ncol = 1)
model <- fit.malps(X, X[, 1]^2, degree = 1L,
                   support.type = "knn", support.size = 8L)
head(fitted(model))
head(residuals(model))
}
\seealso{
\code{\link{refit}}, \code{\link{smoother.matrix}}
}
