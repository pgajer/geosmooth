% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_graph_lowpass.R
\name{refit.metric.graph.lowpass.fit}
\alias{refit.metric.graph.lowpass.fit}
\alias{refit.metric.graph.lowpass}
\title{Refit Metric-Conductance Graph Low-Pass Regression}
\usage{
\method{refit}{metric.graph.lowpass.fit}(
  object,
  y,
  per.column.gcv = FALSE,
  eta.grid = NULL,
  n.candidates = 40L,
  n.cores = 1L,
  block.size = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{"metric.graph.lowpass.fit"} object.}

\item{y}{Numeric vector or matrix with one row per graph vertex.}

\item{per.column.gcv}{Logical. If \code{TRUE}, select eta independently for
each response column using the cached eigenbasis.}

\item{eta.grid}{Optional positive numeric eta grid for per-column GCV.}

\item{n.candidates}{Number of eta candidates when \code{eta.grid = NULL}.}

\item{n.cores}{Number of cores for per-column GCV. The current
implementation uses sequential processing if optional parallel packages
are unavailable.}

\item{block.size}{Optional block size for fixed-eta multi-column refits.}

\item{verbose}{Logical progress flag.}

\item{...}{Additional arguments are not currently supported.}
}
\value{
A list of class \code{"metric.graph.lowpass.refit"}.
}
\description{
Reuses a fitted metric graph low-pass eigensystem to smooth new responses.
}
\examples{
adj <- list(2L, c(1L, 3L), c(2L, 4L), 3L)
lengths <- list(1, c(1, 1), c(1, 1), 1)
fit <- fit.metric.graph.lowpass(
  adj, lengths, y = 1:4, n.eigenpairs = 4L,
  eta.grid = c(0.1, 1), eigen.solver = "dense"
)
refit(fit, y = 4:1)
}
\seealso{
\code{\link{refit}} for supported fitted objects and migration.
}
