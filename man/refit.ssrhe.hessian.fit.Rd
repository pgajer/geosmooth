% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssrhe_hessian_energy.R
\name{refit.ssrhe.hessian.fit}
\alias{refit.ssrhe.hessian.fit}
\alias{refit.ssrhe.hessian.regression}
\title{Refit SSRHE-Style Hessian-Energy Regression}
\usage{
\method{refit}{ssrhe.hessian.fit}(
  object,
  y = NULL,
  lambda1 = object$lambda$lambda1,
  lambda2 = object$lambda$lambda2,
  weights = NULL,
  ridge = object$lambda$ridge,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{"ssrhe.hessian.fit"} object.}

\item{y}{New numeric response vector or matrix with one row per vertex.
If \code{NULL}, the original response is reused.}

\item{lambda1}{Nonnegative Hessian-energy penalty multiplier for
\eqn{f^\top B f = \|Af\|_2^2}.}

\item{lambda2}{Nonnegative supplemental-stabilizer multiplier for
\eqn{f^\top B_S f}. If positive, \code{stabilizer} must be \code{TRUE}.
Currently supported only for \code{derivative.order = 2L}.}

\item{weights}{Optional nonnegative observation weights. May be \code{NULL},
a vector of length \code{nrow(X)}, or a matrix with the same dimensions as
\code{y}.}

\item{ridge}{Nonnegative diagonal ridge added to the linear system for
numerical stabilization.}

\item{verbose}{Logical. If \code{TRUE}, print a short native construction
message.}

\item{...}{Additional arguments are not currently supported.}
}
\value{
A list of class \code{"ssrhe.hessian.refit"}.
}
\description{
Reuses the operator from \code{\link{fit.ssrhe.hessian.regression}} to fit
new responses or new fixed penalty weights without rebuilding local PCA
neighborhoods or Hessian-energy matrices.
}
\examples{
X <- matrix(seq(0, 1, length.out = 12), ncol = 1)
fit <- fit.ssrhe.hessian.regression(
  X, X[, 1]^2, k = 6L, tangent.dim = 1L, lambda1 = 0.1
)
refit(fit, y = X[, 1]^3)
}
\seealso{
\code{\link{refit}} for supported fitted objects and migration.
}
