% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_density.R
\name{fit.density}
\alias{fit.density}
\title{Fit a Density Estimator}
\usage{
fit.density(
  X,
  weights = NULL,
  method = c("empirical", "graph_random_walk"),
  graph = NULL,
  graph.control = list(),
  density.control = list(),
  return.details = TRUE,
  ...
)
}
\arguments{
\item{X}{Numeric matrix with one row per support point.}

\item{weights}{Optional nonnegative mass/count vector of length
\code{nrow(X)}. Required by count-based density methods.}

\item{method}{Density method identifier.}

\item{graph}{Optional precomputed graph object.}

\item{graph.control}{List of graph-method controls.}

\item{density.control}{List controlling clipping, normalization, and
accounting checks.  Recognized entries are \code{mass.tol},
\code{neg.tol}, \code{clip.negative}, and \code{renormalize}.}

\item{return.details}{Logical; if \code{TRUE}, keep diagnostic details in
the result.}

\item{...}{Additional method-specific arguments.}
}
\value{
A list of class \code{"density_fit"} with fields
\code{method.id}, \code{status}, \code{rho}, \code{empirical.rho},
\code{fitted.raw}, \code{theta}, \code{accounting},
\code{smoothness}, \code{timing}, \code{diagnostics}, and
\code{warnings}.
}
\description{
Dispatches to a dedicated density estimator. Subject-occupation density
estimation is one application: construct a sparse mass vector over a fixed
support set and call this generic density layer.
}
\examples{
X <- matrix(seq(0, 1, length.out = 6), ncol = 1)
fit.density(X, weights = c(1, 0, 2, 0, 0, 1), method = "empirical")
}
