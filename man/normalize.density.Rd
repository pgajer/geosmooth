% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_density.R
\name{normalize.density}
\alias{normalize.density}
\alias{normalize.density.numeric}
\alias{normalize.density.default}
\alias{normalize.density.lps}
\alias{normalize.density.ps_lps}
\alias{normalize.density.metric.graph.lowpass.fit}
\alias{normalize.density.metric.graph.lowpass.refit}
\title{Normalize Fitted Values Into a Density}
\usage{
normalize.density(x, ...)

\method{normalize.density}{numeric}(
  x,
  X = NULL,
  density.control = list(),
  method.id = "normalized_numeric",
  keep.source.fit = FALSE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)

\method{normalize.density}{default}(
  x,
  X = NULL,
  density.control = list(),
  method.id = NULL,
  keep.source.fit = TRUE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)

\method{normalize.density}{lps}(
  x,
  X = NULL,
  density.control = list(),
  method.id = NULL,
  keep.source.fit = TRUE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)

\method{normalize.density}{ps_lps}(
  x,
  X = NULL,
  density.control = list(),
  method.id = NULL,
  keep.source.fit = TRUE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)

\method{normalize.density}{metric.graph.lowpass.fit}(
  x,
  X = NULL,
  density.control = list(),
  method.id = NULL,
  keep.source.fit = TRUE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)

\method{normalize.density}{metric.graph.lowpass.refit}(
  x,
  X = NULL,
  density.control = list(),
  method.id = NULL,
  keep.source.fit = TRUE,
  adj.list = NULL,
  empirical.rho = NULL,
  return.details = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or fitted object with a \code{fitted.values} field.}

\item{...}{Additional arguments passed to methods.}

\item{X}{Optional support/evaluation matrix.  If omitted, methods use the
fitted object's stored \code{X.eval} or \code{X} field when available; for
a bare numeric vector, a one-dimensional index support is used.}

\item{density.control}{List controlling clipping, normalization, and
accounting checks.  See \code{\link{fit.density}}.}

\item{method.id}{Character method identifier recorded in the returned object.}

\item{keep.source.fit}{Logical; if \code{TRUE}, retain the source fit in
diagnostics for object methods.}

\item{adj.list}{Optional adjacency list used to compute graph-local
smoothness diagnostics for the normalized density.}

\item{empirical.rho}{Optional empirical probability mass vector used for
accounting diagnostics.}

\item{return.details}{Logical; if \code{TRUE}, keep diagnostic details in
the result.}
}
\value{
A list of class \code{"density_fit"}.
}
\description{
Converts a numeric field or fitted smoother/regression object into a
probability mass vector over the evaluation support.  This is the explicit
adapter between ordinary smoothers such as \code{\link{fit.lps}},
\code{\link{fit.ps.lps}}, and \code{\link{fit.metric.graph.lowpass}} and
density or occupation-density workflows.
}
