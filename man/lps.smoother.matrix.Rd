% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lps_uncertainty.R
\name{lps.smoother.matrix}
\alias{lps.smoother.matrix}
\title{Extract the Linear-Smoother Matrix of a Fixed-Configuration LPS Fit}
\usage{
lps.smoother.matrix(object, check.tol = 1e-10)
}
\arguments{
\item{object}{A fitted \code{"lps"} object from \code{\link[=fit.lps]{fit.lps()}} at a fixed
configuration (see Details).}

\item{check.tol}{Positive scalar: maximum allowed absolute discrepancy of
the self-guard identity \code{S \%*\% y == fitted.values.raw}. Default \code{1e-10},
the program's algebraic tolerance.}
}
\value{
A numeric matrix \code{S} with \code{nrow(object$X.eval)} rows and
\code{nrow(object$X)} columns: row \code{i} holds the weights through which the
training responses enter the prediction at evaluation point \code{i}.
}
\description{
For a fixed configuration (singleton \code{support.grid}, \code{degree.grid}, and
\code{kernel.grid}, with an explicit numeric chart dimension) the LPS fitted
vector is linear in the response: \code{fitted = S \%*\% y} with \code{S} depending on
\code{X}, the kernel weights, and the configuration, but not on \code{y}. This
function reconstructs \code{S} analytically, one evaluation row at a time, by
rebuilding each local fit's support, kernel weights, local chart, and
design through the same internal routines \code{fit.lps()} used, and reading off
the influence row of the local weighted least-squares solve.
}
\details{
The extraction refuses configurations where the linearity premise fails or
is unsupported: it requires \code{outcome.family = "gaussian"}, the R backend,
\code{design.basis = "orthogonal.polynomial.drop"}, singleton grids (no
data-driven selection: the CV-selected pipeline is \emph{not} a linear
smoother), and \code{coordinate.method = "coordinates"} or \code{"local.pca"} with an
explicit numeric \code{chart.dim} (never \code{"auto"} or \code{"local.auto"}).

Self-guard: before returning, the function verifies
\code{max(abs(S \%*\% y - fitted.values.raw)) <= check.tol} against the fit it was
given (and that the \code{NA} patterns agree), so any divergence between the
reconstruction and the estimator is a hard error, never a silent drift.

Local fits that fell back are represented honestly: a weighted-mean
fallback (\code{unstable.action = "mean"}) contributes its exact linear row
\code{w / sum(w)}; an \code{unstable.action = "na"} non-fit contributes an all-\code{NA}
row.
}
\examples{
set.seed(1)
n <- 30
X <- matrix(runif(2 * n, -1, 1), ncol = 2)
y <- sin(pi * X[, 1]) + 0.1 * rnorm(n)
fit <- fit.lps(X, y, foldid = rep(1:2, length.out = n),
               support.grid = 12L, degree.grid = 1L,
               kernel.grid = "tricube", backend = "R",
               design.basis = "orthogonal.polynomial.drop",
               ridge.multiplier.grid = 0, ridge.condition.max = Inf,
               unstable.action = "na")
S <- lps.smoother.matrix(fit)
max(abs(S \%*\% y - fit$fitted.values))   # ~1e-15: the linear identity
sum(diag(S))                            # effective degrees of freedom
}
\seealso{
\code{\link[=lps.pointwise.band]{lps.pointwise.band()}} for pointwise variances and confidence
bands derived from \code{S}.
}
