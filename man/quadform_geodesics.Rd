% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadform_geodesics.R
\name{quadform_geodesics}
\alias{quadform_geodesics}
\title{Estimate distances on a declared quadratic graph surface}
\usage{
quadform_geodesics(
  A,
  from,
  to,
  domain,
  method,
  control = list(),
  return.paths = FALSE
)
}
\arguments{
\item{A}{Finite exactly symmetric 2 by 2 matrix.}

\item{from, to}{Two-element numeric vectors or matching two-column matrices
of domain coordinates. Rows define pairs; no recycling or Cartesian product.}

\item{domain}{A list with \code{kind = "ball"}, \code{center} and \code{radius}, or
\code{kind = "box"}, \code{lower} and \code{upper}. The domain is closed and convex.}

\item{method}{One of \code{"single_point"}, \code{"local_network"}, \code{"best_of_six"},
\code{"grid_dijkstra"} or \code{"paraboloid_clairaut"}. No partial matching.}

\item{control}{Named list of method-specific controls; unknown names are
errors. Refinements accept the corresponding lower-level function's
controls, with \code{cache_edges = 0} and \code{trace = FALSE} by default.
Grid controls are \code{grid_size = c(33,33)} (3--257 per axis),
\code{direction_radius = 2} (1--8), \code{endpoint_neighbors = 8} (1--32),
\code{include_direct = TRUE}, \code{max_edges = 1000000} (0--2000000),
\code{keep_graph = FALSE} and \code{max_seconds = Inf}.
Clairaut controls are \code{iterations = 80} (1--256), \code{path_samples = 257}
(3--4097), \code{domain_check_depth = 16} (0--20),
\code{angle_tolerance = 1e-12} (1e-15--1e-8) and \code{max_seconds = Inf}.
Time and calculation limits apply to each pair, not the whole batch.}

\item{return.paths}{Whether to retain paths, including those in nested
refinement results. Default \code{FALSE}. No files or checkpoints are written.}
}
\value{
A list with \code{method}, \code{geometry}, \code{domain}, resolved \code{control},
row-preserving \code{summary}, and detailed pair \code{results}. Status is
\code{"candidate"}, \code{"partial"}, \code{"unsupported"} or \code{"failed"}.
Summary \code{length} is the numerical distance estimate; \code{length_error_estimate}
concerns numerical length evaluation, not discretization or optimization
error. \code{approximation_error} is missing and \code{global_optimality_certified}
is \code{FALSE}. Detailed results retain \code{backend_result}, including all six
outcomes and total costs for the best-of-six strategy. A completed budget
is not an accuracy certificate. User interrupts and input errors propagate.
}
\description{
Internal common interface for paired endpoint queries on
\eqn{F(u)=(u,u^T A u)}. A method must be selected explicitly.
}
\details{
Grid edges use the native analytic lifted-segment lengths. Primitive
lattice offsets up to \code{direction_radius} supply directions; radius one is
the eight-neighbor stencil. Finer spacing alone does not remove its
directional bias. Off-grid endpoints attach to nearest retained vertices.
The optional direct connector is an explicit graph edge, not a fallback.
Search uses strict binary64 comparisons; the selected path total is
recomputed with the native exact accumulator.

Clairaut requires \verb{A = a I} exactly. It solves radial-monotone or turning
branches of the continuous surface metric, with separate flat, apex,
radial and antipodal cases. Origin-centered disks use a whole-path radial
envelope; other domains require conservative radial/angular section
enclosures. Leaving the domain or unresolved containment returns
\code{unsupported}, not a boundary-constrained replacement. Curved polar cases
outside the initial numeric scope (minimum endpoint radius below 1e-100,
maximum above 1e100, or twice absolute curvature times maximum radius above
1e6) are explicitly unsupported. These restrictions do not apply to flat,
coincident or apex cases.

For \code{analytic_clairaut_curve}, \code{path} is a sampled display of the curve,
not a polyline whose length equals \code{length}. \code{curve_parameters} describe
the authoritative curve; the numerical endpoint residual is retained.
Parameterize it by \code{t = t_from + s * t_span}, for \code{s} between zero and
one; use the retained \code{t_span}, not subtraction of rounded endpoints.
With \eqn{r = \sqrt{c^2 + t^2}}, its polar angle is \code{theta_turn} plus
\code{angular_sign * sign(t) * H(c, abs(t))}, where
\eqn{H(c,t)=kc\,\mathrm{asinh}(kt/\sqrt{1+k^2c^2})+
  \mathrm{atan2}(t,c\sqrt{1+k^2c^2+k^2t^2})}.
\code{caller_reversed} indicates whether to traverse this parameterization
backward to follow the caller's endpoint order.
For \code{lifted_domain_polyline}, consecutive domain points specify lifted
straight segments. All numerical error estimates are nonrigorous.
The historical development adapter registry is independent of this
package interface and is not activated by these methods.
Grid and Clairaut results are symmetric under endpoint reversal. A single
randomized refinement run is not required to be: its internal orientation
is randomized relative to the supplied endpoints. The wrapper preserves
the lower-level solver's behavior and seed semantics.
}
\keyword{internal}
