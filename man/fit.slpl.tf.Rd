% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slpl_tf.R
\name{fit.slpl.tf}
\alias{fit.slpl.tf}
\title{Fit A Fixed-Operator Synchronized LPL-TF Model}
\usage{
fit.slpl.tf(
  X = NULL,
  y,
  operator = NULL,
  lambda1 = NULL,
  lambda2 = 0,
  lambda1.grid = NULL,
  lambda2.grid = NULL,
  sync.lambda.grid = NULL,
  lambda.selection = c("fixed", "cv"),
  operator.grid = NULL,
  foldid = NULL,
  cv.folds = 5L,
  cv.loss = c("mse", "rmse", "mae"),
  cv.seed = NULL,
  cv.repeats = 1L,
  solver = c("genlasso"),
  selection = c("min", "one.se"),
  maxsteps = 2000L,
  minlam = 0,
  approx = FALSE,
  rtol = 1e-07,
  btol = 1e-07,
  eps = 1e-04,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{X}{Optional coordinate matrix used to build
\code{\link{slpl.tf.operator}} when \code{operator} is \code{NULL}.}

\item{y}{Numeric response vector.}

\item{operator}{Optional \code{"slpl_tf_operator"} object.}

\item{lambda1}{Fixed LPL-TF \eqn{\ell_1} penalty, or a shortcut for
\code{lambda1.grid} when \code{lambda.selection = "cv"}.}

\item{lambda2}{Fixed quadratic synchronization penalty, or a shortcut for
\code{lambda2.grid} when \code{lambda.selection = "cv"}.}

\item{lambda1.grid}{Optional nonnegative \eqn{\lambda_1} grid for CV.}

\item{lambda2.grid}{Optional nonnegative \eqn{\lambda_2} grid for CV.}

\item{sync.lambda.grid}{Deprecated alias for \code{lambda2.grid}.}

\item{lambda.selection}{\code{"fixed"} or \code{"cv"}. CV uses materialized
folds and a deterministic Cartesian grid over \code{lambda1.grid} and
\code{lambda2.grid}.}

\item{operator.grid}{Optional Phase-S3 operator candidate grid. Supply a data
frame with one row per candidate or a list of named lists.}

\item{foldid}{Optional deterministic fold assignments for CV.}

\item{cv.folds}{Number of generated folds when \code{foldid = NULL}.}

\item{cv.loss}{Cross-validation loss, \code{"mse"}, \code{"rmse"}, or
\code{"mae"}.}

\item{cv.seed}{Optional seed for reproducible generated folds.}

\item{cv.repeats}{Phase S3 supports only one CV repeat.}

\item{solver}{Current implementation supports only \code{"genlasso"}.}

\item{selection}{Selection rule, \code{"min"} or \code{"one.se"}. For the
two-parameter one-standard-error rule, the most regularized eligible pair
is chosen by decreasing \code{lambda1} and then decreasing \code{lambda2}.}

\item{maxsteps, minlam, approx, rtol, btol, eps}{Genlasso controls.}

\item{verbose}{Logical.}

\item{...}{Additional arguments passed to \code{\link{slpl.tf.operator}} when
the operator is built internally.}
}
\value{
A list of class \code{"slpl_tf"}.
}
\description{
Fits the fixed-operator S-LPL-TF estimator
\deqn{\hat f = \arg\min_f \frac12\|y-f\|_2^2 +
\lambda_1\|A_{\mathrm{LPL}}f\|_1 +
\frac{\lambda_2}{2}\|C_{\mathrm{sync}}f\|_2^2.}
}
