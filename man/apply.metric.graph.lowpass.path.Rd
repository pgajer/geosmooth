% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_graph_lowpass.R
\name{apply.metric.graph.lowpass.path}
\alias{apply.metric.graph.lowpass.path}
\title{Apply a Metric Graph Low-Pass Filter Path}
\usage{
apply.metric.graph.lowpass.path(
  basis,
  y,
  eta.grid,
  filter.type = c("heat_kernel", "tikhonov", "cubic_spline", "gaussian", "exponential",
    "butterworth"),
  block.size = NULL,
  truncation.tol = 1e-04,
  unresolved.action = c("warn", "error", "allow"),
  exact.zero = TRUE
)
}
\arguments{
\item{basis}{A \code{"metric.graph.lowpass.basis"} object.}

\item{y}{Numeric response vector or matrix with one row per graph vertex.}

\item{eta.grid}{Numeric filter-parameter grid.}

\item{filter.type}{Spectral low-pass filter family.}

\item{block.size}{Optional number of response columns processed together.}

\item{truncation.tol}{Positive tolerance smaller than one used to classify
truncated-basis candidates as spectrally resolved.}

\item{unresolved.action}{Action when a truncated-basis candidate exceeds
\code{truncation.tol}: \code{"warn"}, \code{"error"}, or \code{"allow"}.}

\item{exact.zero}{Logical. For heat filtering, return the input exactly at
\code{eta = 0}. This avoids treating a truncated spectral projection as
the identity.}
}
\value{
A list of class \code{"metric.graph.lowpass.path"}. For one response,
\code{fitted.values} is an \eqn{N} by \eqn{J} matrix. For multiple
responses it is an \eqn{N} by \eqn{J} by \eqn{S} array.
}
\description{
Applies every requested low-pass parameter to one response or a matrix of
responses using a reusable spectral basis.
}
\examples{
adj <- list(2L, c(1L, 3L), c(2L, 4L), 3L)
lengths <- list(1, c(1, 1), c(1, 1), 1)
basis <- metric.graph.lowpass.basis(adj, lengths, n.eigenpairs = 4L,
                                    eigen.solver = "dense")
apply.metric.graph.lowpass.path(basis, 1:4, eta.grid = c(0, 0.1, 1))
}
