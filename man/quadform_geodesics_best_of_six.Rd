% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadform_geodesics_best_of_six.R
\name{quadform_geodesics_best_of_six}
\alias{quadform_geodesics_best_of_six}
\title{Select a path from six quadratic-surface refinement searches}
\usage{
quadform_geodesics_best_of_six(
  A,
  from,
  to,
  domain,
  seed = 1,
  length_scale = 1,
  initial_edges = 16L,
  candidates = 32L,
  max_epochs = 64L,
  max_vertices = 257L,
  max_edge_calls = Inf,
  max_seconds = Inf,
  cache_edges = 0L,
  rejection_cap = 1000L,
  random_orientation = TRUE,
  trace = FALSE,
  trace_limit = 1000L
)
}
\arguments{
\item{A}{Finite symmetric 2 by 2 quadratic-form matrix.}

\item{from, to}{Numeric vectors of two domain coordinates, inside \code{domain}.}

\item{domain}{A list with \code{kind = "ball"}, numeric \code{center} and positive
\code{radius}, or \code{kind = "box"} and numeric \code{lower} and \code{upper} bounds.}

\item{seed}{Whole-number seed between zero and \eqn{2^{32}-1}. Dedicated
C++ streams do not create or modify R's random state.}

\item{length_scale}{Positive surface-length reference for absolute tolerances.}

\item{initial_edges}{Initial segment count, between 2 and 256.}

\item{candidates}{Accepted disk draws per visit, between 1 and 256. Draws
outside the domain are rejected; exact duplicates are removed afterwards.}

\item{max_epochs}{Rounds per search, default 64. Plateau stopping is disabled.}

\item{max_vertices}{Maximum path size, at most 257 and greater than
\code{initial_edges}.}

\item{max_edge_calls, max_seconds}{Safety allowances for each of the six
searches, not combined allowances. Defaults are unlimited; rounds still
bound the search. Interrupted searches retain their original statuses.}

\item{cache_edges}{Cache capacity per search; default zero.}

\item{rejection_cap}{Maximum attempts per accepted candidate or shuffle draw.}

\item{random_orientation}{Randomize internal endpoint orientation once per
call. Returned paths always follow the caller's endpoint order.}

\item{trace}{Retain a bounded in-memory refinement trace. Default \code{FALSE}.}

\item{trace_limit}{Maximum retained trace events, between zero and 10000.}
}
\value{
The selected solver result, with an additional \code{ensemble} list.
\code{ensemble$results} retains all six solver results and \code{ensemble$summary}
records methods, rules, seeds, lengths, error estimates, statuses, round
completion and costs. \code{selected_index} identifies the minimum estimated
length; exact length ties prefer the earlier row. \code{tied_indices} also
includes usable results whose difference from the selected length is no
larger than their combined estimated errors. These estimates are not
rigorous intervals. Numerical ties never cause a longer estimated path
to replace the minimum. \code{status} within \code{ensemble} is \code{"complete"} when
all six round budgets complete (or endpoints coincide), \code{"partial"} when
at least one usable path is available but some search is incomplete, and
\code{"no_path"} when none is usable. In the latter case \code{selected_index} is
missing and the top-level result is the first no-path solver result.
Top-level counters describe the selected search only; total cost is in
\code{ensemble$total_edge_calls}, \code{total_solver_seconds}, and
\code{total_call_seconds}. Input errors and unexpected R errors propagate.
}
\description{
Run both refinement methods with all three neighborhood rules and return
the shortest usable result. Each search starts afresh from the same seeded
equal-surface-length initialization. This is a finite-budget heuristic,
not a certificate of a globally shortest surface path.
}
\details{
Searches use the same seed to preserve their initialization, but
may diverge in their candidate draws and visits as their paths change.
They execute serially and do not change R's random state. Parallelize
independent endpoint pairs in separate R processes rather than nesting
workers inside this function. No state files are saved.
}
\keyword{internal}
