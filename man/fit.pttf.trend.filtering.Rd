% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pttf_fit.R
\name{fit.pttf.trend.filtering}
\alias{fit.pttf.trend.filtering}
\title{Fit A Parallel-Transport Trend Filter From A PTTF Operator}
\usage{
fit.pttf.trend.filtering(
  geometry = NULL,
  operator = NULL,
  X = NULL,
  y,
  derivative.order = 3L,
  penalty = c("l1", "l2"),
  lambda.grid = NULL,
  lambda.selection = c("cv", "fixed"),
  n.lambda = 80L,
  nfolds = 5L,
  foldid = NULL,
  cv.loss = c("mse", "mae"),
  selection = c("min", "one.se"),
  lambda.extension = c("none", "auto"),
  solver = c("genlasso", "admm", "auto"),
  operator.row.policy = c("all", "drop.line.boundary", "diagnostic.only"),
  line.order = NULL,
  boundary.trim = NULL,
  row.mass.rule = c("none", "node.mass"),
  row.normalize = c("none", "l2"),
  weights = NULL,
  maxsteps = 2000L,
  minlam = 0,
  approx = FALSE,
  rtol = 1e-07,
  btol = 1e-07,
  eps = 1e-04,
  admm.rho = 1,
  admm.maxiter = 2000L,
  admm.abstol = 1e-04,
  admm.reltol = 0.001,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{geometry}{Optional \code{"pttf_geometry"} object. Used only when
\code{operator} is \code{NULL}.}

\item{operator}{Optional \code{"pttf_operator"} object. Supplying an
operator is recommended for validation so geometry/operator construction is
separated from fitting.}

\item{X}{Optional data matrix used to build \code{\link{pttf.geometry}} when
both \code{geometry} and \code{operator} are \code{NULL}.}

\item{y}{Numeric response vector.}

\item{derivative.order}{Integer derivative order passed to
\code{\link{pttf.operator}} when an operator must be built.}

\item{penalty}{\code{"l1"} for generalized-lasso style fitting or
\code{"l2"} for the quadratic energy fit.}

\item{lambda.grid}{Optional lambda grid. Required for
\code{lambda.selection = "fixed"}.}

\item{lambda.selection}{\code{"cv"} or \code{"fixed"}.}

\item{n.lambda}{Number of lambdas in a generated grid.}

\item{nfolds}{Number of folds used only when \code{foldid = NULL}.}

\item{foldid}{Optional integer fold assignments. Validation lanes should pass
a materialized \code{foldid}; otherwise folds are generated by R's current
RNG state.}

\item{cv.loss}{Cross-validation loss, \code{"mse"} or \code{"mae"}.}

\item{selection}{Lambda selection rule, \code{"min"} or \code{"one.se"}.}

\item{lambda.extension}{Currently recorded as metadata. Phase 3 implements no
new extension heuristic.}

\item{solver}{L1 solver backend.}

\item{operator.row.policy}{Which operator rows to use for fitting.}

\item{line.order}{Vertex order for 1-D boundary-row policies.}

\item{boundary.trim}{Number of endpoint ranks to drop for
\code{operator.row.policy = "drop.line.boundary"}. Defaults to
\code{derivative.order}.}

\item{row.mass.rule, row.normalize}{Passed to \code{\link{pttf.operator}} when
this function builds an operator. \code{row.normalize} is also used as the
L1 solver row scaling for the supplied fit operator.}

\item{weights}{Optional nonnegative observation weights.}

\item{maxsteps, minlam, approx, rtol, btol, eps}{Genlasso controls for L1 fits.}

\item{admm.rho, admm.maxiter, admm.abstol, admm.reltol}{ADMM controls used when
the L1 backend is \code{"admm"} or falls back to ADMM.}

\item{verbose}{Logical.}

\item{...}{Additional arguments passed to \code{\link{pttf.geometry}} or
\code{\link{pttf.operator}} when those objects are built internally.}
}
\value{
A list of class \code{"pttf.trend.filtering.fit"}.
}
\description{
Fits the first experimental PTTF regression models from a transported
difference operator produced by \code{\link{pttf.operator}}. Phase 3 focuses
on one response vector and explicit operator-row policies, especially the
1-D boundary policy used to separate interior transported rows from endpoint
rows.
}
