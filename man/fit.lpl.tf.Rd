% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpl_tf.R
\name{fit.lpl.tf}
\alias{fit.lpl.tf}
\title{Fit A Local Polynomial Lifting Trend Filter}
\usage{
fit.lpl.tf(
  X = NULL,
  y,
  operator = NULL,
  adj.list = NULL,
  weight.list = NULL,
  graph = NULL,
  degree = 2L,
  lambda.grid = NULL,
  lambda = NULL,
  lambda.selection = c("cv", "fixed"),
  operator.grid = NULL,
  foldid = NULL,
  cv.folds = 5L,
  cv.loss = c("mse", "rmse", "mae"),
  cv.seed = NULL,
  cv.repeats = 1L,
  solver = c("genlasso"),
  selection = c("min", "one.se"),
  n.lambda = 80L,
  maxsteps = 2000L,
  minlam = 0,
  approx = FALSE,
  rtol = 1e-07,
  btol = 1e-07,
  eps = 1e-04,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{X}{Optional coordinate matrix used to build \code{\link{lpl.tf.operator}}
when \code{operator} is \code{NULL}.}

\item{y}{Numeric response vector.}

\item{operator}{Optional \code{"lpl_tf_operator"} object.}

\item{adj.list, weight.list, graph}{Optional graph inputs passed to
\code{\link{lpl.tf.operator}} when \code{operator} is \code{NULL}.}

\item{degree}{Polynomial degree passed to \code{\link{lpl.tf.operator}} when
\code{operator} is \code{NULL}.}

\item{lambda.grid}{Optional nonnegative lambda grid.}

\item{lambda}{Optional fixed lambda shortcut. If supplied, it is used as the
single fixed lambda and \code{lambda.selection} must be \code{"fixed"}.}

\item{lambda.selection}{\code{"cv"} or \code{"fixed"}. Phase 2 requires an
explicit \code{lambda.grid} for CV so the candidate grid is not generated
from the full response vector before fold scoring.}

\item{operator.grid}{Optional Phase-3 operator candidate grid. Supply a data
frame with one row per candidate or a list of named lists. Candidate fields
may include operator-construction arguments such as \code{degree},
\code{support.type}, \code{support.size}, \code{min.support},
\code{support.buffer}, \code{kernel}, \code{row.normalize}, and
\code{local.solver}.}

\item{foldid}{Optional deterministic fold assignments for CV.}

\item{cv.folds}{Number of generated folds when \code{foldid = NULL}.}

\item{cv.loss}{Cross-validation loss, \code{"mse"}, \code{"rmse"}, or
\code{"mae"}. \code{"rmse"} uses the same lambda ordering as MSE and reports
square-rooted fold errors.}

\item{cv.seed}{Reserved for reproducible generated folds. Phase 2 generated
folds are deterministic even when this is \code{NULL}.}

\item{cv.repeats}{Phase 2 supports only one CV repeat.}

\item{solver}{Phase 2 supports only \code{"genlasso"}.}

\item{selection}{Lambda selection rule, \code{"min"} or \code{"one.se"}.}

\item{n.lambda}{Number of generated lambdas when a genlasso path is used and
\code{lambda.grid = NULL}.}

\item{maxsteps, minlam, approx, rtol, btol, eps}{Genlasso controls.}

\item{verbose}{Logical.}

\item{...}{Additional arguments passed to \code{\link{lpl.tf.operator}} when
the operator is built internally.}
}
\value{
A list of class \code{"lpl_tf"}.
}
\description{
Fits the Phase-2 LPL-TF estimator with a fixed operator:
\deqn{\hat f = \arg\min_f \frac12\|y-f\|_2^2 +
\lambda\|A_{\mathrm{LPL}}f\|_1.}
}
